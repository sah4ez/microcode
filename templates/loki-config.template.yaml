# Reference template — the actual loki-config.yaml is generated by
# microcode.generators.loki from the loki section of platform.yaml.
provider: claude
effort: high
max_iterations: 20
max_budget_usd: 10.0
sdk_mode: full
quality_gates:
  enabled: true
  opt_out: []
memory:
  enabled: true
  managed: true
proofs:
  enabled: true
